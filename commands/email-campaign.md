---
description: Start an email marketing campaign — brief to a reviewable draft in your CLM (Klaviyo, Mailchimp, etc.)
argument-hint: [brief, goal, or "welcome series for <brand>"]
---

Use the **email-campaign** skill to run the full workflow for the user's request below.

Request: $ARGUMENTS

Begin at Phase 1 (Intake). Honor the skill's core principles — especially:
- **Brand-agnostic until the design phase**; the brand URL is only a pointer in the brief.
- **Defer all CLM/MCP calls to Phase 7.**
- **Never auto-send** — create drafts/paused flows only, and confirm recipient count + audience before any send.

If the request is clearly a single email to *design only* (not deliver), offer the fast path:
follow the design reference `references/email-design.md` and stop.
